
<div class='row'>
  <div class='span12'>

      <div class='alert alert-info'>
        <strong>Installation -- Site Owner Creation</strong>
        <br />
        Before we can begin we need to create an admin user that you'll use to login and edit things with.  <br />
        Once you have created this user you'll be asked to sign in and forwarded to the settings area.
      </div>

      <%= form_for @user, :url => create_owner_install_index_path, :html => { :class => 'form-horizontal' } do |f| %>

        <%= render :partial => 'owner_form', :locals => {
                :f => f,
                :form_legend => "Create Site Owner",
                :submit_label => "Create Owner"
        } %>

      <% end %>

  </div>
</div>