<% @breadcrumbs = [link_to('Content Editor', admin_content_index_path(:pages)), 'New Page'] %>

<div class='row'>

  <%= form_for @content, :url => admin_pages_path(@content), :html => { :class => '' } do |f| %>

    <%= render :partial => 'page_form', :locals => {
            :f => f,
            :form_legend => "Create new page",
            :submit_label => "Create Page"
    } %>

  <% end %>

</div>
