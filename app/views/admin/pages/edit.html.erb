<%= breadcrumbs :controller => { :name => 'Content', :link => admin_content_index_path(:pages) } %>

<div class='row'>

  <%= form_for @content, :url => admin_page_path(@content), :html => { :class => '' } do |f| %>

    <%= render :partial => 'page_form', :locals => {
            :f => f,
            :form_legend => "Edit page",
            :submit_label => "Update Page"
    } %>

  <% end %>

</div>