<% @breadcrumbs = [link_to('Posts Editor', {:action => 'index'}), 'Posts List'] %>

<table class='table table-striped'>
  <thead>
    <tr>
      <th>Title</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= link_to post.title, {} %></td>
        <td>
          <%= link_to 'Edit', {:action => 'edit', :id => post.id}, :class => 'btn btn-mini' %>
          <%= link_to 'Destroy', {:action => 'destroy', :id => post.id}, :confirm => "You've chosen to delete this post, this can't be undone. Are you sure?", :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to({:action => 'new'}, :class => 'btn btn-primary') do %>
    <i class='icon-plus icon-white'></i> Add New Post
<% end %>
