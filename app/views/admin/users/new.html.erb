<% @breadcrumbs = [link_to('Users Editor', {:action => 'index'}), 'Add New User'] %>

<div class='row'>
  <div class='span12'>

    <%= form_for [:admin, @user], :html => { :class => 'form-horizontal' } do |f| %>

      <%= render :partial => 'user_form', :locals => {
              :f => f,
              :form_legend => "Create new user",
              :submit_label => "Create User"
      } %>

    <% end %>

  </div>
</div>