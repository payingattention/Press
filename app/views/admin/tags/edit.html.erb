<% @breadcrumbs = [link_to('Tag Editor', {:action => 'index'}), 'Edit Tag'] %>

<div class='row'>
  <div class='span12'>

    <%= form_for [:admin, @tag], :html => { :class => 'form-horizontal' } do |f| %>

      <%= render :partial => 'form', :locals => {
              :f => f,
              :form_legend => "Edit tag",
              :submit_label => "Update tag"
      } %>

    <% end %>

  </div>
</div>